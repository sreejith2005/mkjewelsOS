"use client";

import Link from "@/next-shim/link"; // crm-port: next/link -> local shim (same hrefs, /crm base path added)
import { useRouter } from "@/next-shim/navigation"; // crm-port: next/navigation -> local shim (same paths, /crm base path added)
import { type FormEvent, useEffect, useRef, useState } from "react";

import { lookupClientByPhone } from "@/lib/client-phone-lookup";
import { phoneDigits } from "@/lib/clients";
import { createClient } from "@/lib/supabase/client";

type Item = { id: string; token: string; client_name: string; mobile: string; assigned_crm_name: string | null; status: string; created_at: string; client_id: string | null };
type Branch = { id: string; name: string };

export function EntryQueue({ profile, selectedBranchId, selectedCrm, branches, crms, queueCrms, initialItems, completedName }: { profile: { role: string; branchId: string | null }; selectedBranchId: string; selectedCrm: string; branches: Branch[]; crms: string[]; queueCrms: string[]; initialItems: Item[]; completedName?: string }) {
  const router = useRouter(); const queueRef = useRef<HTMLElement>(null);
  const [name, setName] = useState(""); const [mobile, setMobile] = useState(""); const [branch, setBranch] = useState(selectedBranchId); const [queueCrm, setQueueCrm] = useState(selectedCrm); const [queueTab, setQueueTab] = useState<"active" | "recent">("active"); const [message, setMessage] = useState(""); const [autoFilled, setAutoFilled] = useState(false); const [saving, setSaving] = useState(false);
  useEffect(() => { if (phoneDigits(mobile).length !== 10) return; const timer = window.setTimeout(() => { void lookupClientByPhone(mobile).then((client) => { if (!client) { setAutoFilled(false); return; } setName(client.primary_name); setAutoFilled(true); }); }, 350); return () => window.clearTimeout(timer); }, [mobile]);
  function openQueue() { if (typeof queueRef.current?.scrollIntoView === "function") queueRef.current.scrollIntoView({ behavior: "smooth", block: "start" }); }
  async function submit(event: FormEvent) { event.preventDefault(); if (!name.trim() || phoneDigits(mobile).length !== 10 || !branch) { setMessage("Name, 10-digit mobile, and branch are required."); return; } setSaving(true); const client = createClient(); const { data, error } = await client.rpc("create_entry_queue", { p_client_name: name, p_mobile: mobile, p_branch_id: branch }); setSaving(false); if (error || !data?.[0]) { setMessage("Could not register the queue entry. Check the selected branch."); return; } let clientCode = typeof data[0].client_code === "string" ? data[0].client_code : ""; if (!clientCode && data[0].client_id) { const { data: clientRow } = await client.from("clients").select("client_code").eq("client_id", data[0].client_id).maybeSingle(); clientCode = clientRow?.client_code ?? ""; } if (!clientCode) { setMessage("Client registered, but its client ID is temporarily unavailable. Refresh the queue before continuing."); return; } setMessage(data[0].client_type === "existing" ? `Existing client added to the CRM queue. Client ID: ${clientCode}.` : `Client registered. Client ID: ${clientCode}.`); router.refresh(); window.setTimeout(openQueue, 0); }
  function loadQueue() { const params = new URLSearchParams(); if (branch) params.set("branch", branch); if (queueCrm) params.set("crm", queueCrm); router.push(`/queue${params.size ? `?${params}` : ""}`); window.setTimeout(openQueue, 0); }
  const visibleItems = initialItems.filter((item) => queueTab === "active" ? item.status !== "complete" : item.status === "complete");
  const branchSelect = (id: string, onChange: (value: string) => void, label: string) => <label className="block text-sm"><span>{label}</span><select aria-label={label} className="mt-1 w-full rounded border border-stone-300 bg-white p-2" value={id} disabled={profile.role !== "super_admin"} onChange={(event) => onChange(event.target.value)}>{!id ? <option value="">SELECT BRANCH</option> : null}{branches.map((item) => <option value={item.id} key={item.id}>{item.name}</option>)}</select></label>;
  return <div className="mt-6 space-y-8"><section className="rounded-xl border bg-white p-5" aria-labelledby="register-client-entry"><h2 id="register-client-entry" className="text-lg font-semibold">REGISTER CLIENT ENTRY</h2><form onSubmit={submit} className="mt-4 grid gap-4 md:grid-cols-3">{branchSelect(branch, setBranch, "BRANCH")}<label className="block text-sm"><span>CLIENT NAME</span><input className={`mt-1 w-full rounded border p-2 ${autoFilled ? "border-amber-400 bg-amber-50" : "border-stone-300 bg-white"}`} value={name} onChange={(event) => { setName(event.target.value); setAutoFilled(false); }} />{autoFilled ? <small className="mt-1 block text-amber-800">Auto-filled from client history — editable</small> : null}</label><label className="block text-sm"><span>MOBILE NUMBER</span><div className="mt-1 flex rounded border border-stone-300 bg-white"><span className="border-r border-stone-300 px-3 py-2 text-stone-600">+91</span><input aria-label="Mobile Number" className="min-w-0 flex-1 rounded-r p-2" inputMode="numeric" value={mobile} onChange={(event) => { setMobile(event.target.value); setAutoFilled(false); }} /></div></label><div className="flex flex-wrap items-end gap-3 md:col-span-3"><button disabled={saving} className="rounded bg-amber-800 px-4 py-2 font-medium text-white disabled:opacity-50">{saving ? "REGISTERING…" : "REGISTER CLIENT"}</button><button type="button" onClick={openQueue} className="rounded border border-stone-300 px-4 py-2 font-medium">OPEN CRM QUEUE</button>{message ? <p role="status" className="text-sm text-green-800">{message}</p> : null}</div></form></section><section ref={queueRef} className="overflow-hidden rounded-xl border bg-white" aria-labelledby="crm-queue"><div className="border-b p-5"><h2 id="crm-queue" className="text-lg font-semibold">CRM QUEUE</h2><div className="mt-4 flex flex-wrap gap-2"><button type="button" onClick={() => setQueueTab("active")} className={queueTab === "active" ? "rounded bg-amber-800 px-3 py-2 text-xs font-semibold text-white" : "rounded border px-3 py-2 text-xs font-semibold"}>Active queue</button><button type="button" onClick={() => setQueueTab("recent")} className={queueTab === "recent" ? "rounded bg-amber-800 px-3 py-2 text-xs font-semibold text-white" : "rounded border px-3 py-2 text-xs font-semibold"}>Recently submitted</button></div><div className="mt-4 grid gap-4 md:grid-cols-[minmax(0,1fr)_minmax(0,1fr)_auto]"><label className="block text-sm">CRM NAME<select className="mt-1 w-full rounded border border-stone-300 bg-white p-2" value={queueCrm} onChange={(event) => setQueueCrm(event.target.value)}><option value="">SELECT CRM</option>{queueCrms.map((item) => <option value={item} key={item}>{item}</option>)}</select></label><div>{branchSelect(branch, setBranch, "BRANCH")}</div><button type="button" onClick={loadQueue} className="self-end rounded bg-stone-800 px-4 py-2 text-sm font-medium text-white">LOAD QUEUE</button></div><p className="mt-3 text-sm text-stone-600">{crms.length ? `CRM PRESENT TODAY: ${crms.join(", ")}` : "NO CRM FOUND FOR SELECTED BRANCH. ADD CRM IN CRM PRESENT TODAY."}</p>{completedName ? <p role="status" className="mt-3 text-sm font-medium text-green-800">Visit logged for {completedName}. The queue entry is complete and is available under Recently submitted.</p> : null}</div>{visibleItems.length ? <div className="overflow-x-auto"><table className="w-full min-w-[640px] text-left text-sm"><thead className="bg-stone-50 text-stone-600"><tr><th className="p-4">ENTRY TIME</th><th className="p-4">CLIENT NAME</th><th className="p-4">MOBILE</th><th className="p-4">BRANCH</th><th className="p-4">CRM</th><th className="p-4">TYPE</th><th className="p-4">ACTION</th></tr></thead><tbody>{visibleItems.map((item) => <tr key={item.id} className="border-t"><td className="p-4 font-medium">{new Date(item.created_at).toLocaleString("en-IN", { timeZone: "Asia/Kolkata" })}</td><td className="p-4">{item.client_name}</td><td className="p-4">+91 {item.mobile}</td><td className="p-4">{branches.find((itemBranch) => itemBranch.id === branch)?.name ?? ""}</td><td className="p-4">{item.assigned_crm_name ?? "NA"}</td><td className="p-4">{item.client_id ? "EXISTING" : "NEW"}</td><td className="p-4">{item.status === "complete" ? item.client_id ? <Link className="font-medium text-amber-800 underline" href={`/clients/${item.client_id}`}>Review client record</Link> : <span className="crm-status crm-status--done">Done</span> : <Link className="font-medium text-amber-800 underline" href={`/visits/new?queue=${item.id}`}>{item.client_id ? "MAKE WALK-IN ENTRY" : "ADD WALKIN ENTRY"}</Link>}</td></tr>)}</tbody></table></div> : <p className="p-5 text-sm text-stone-600">NO PENDING CLIENTS FOUND.</p>}</section></div>;
}
