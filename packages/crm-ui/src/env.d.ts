/// <reference types="vite/client" />
// crm-port: Vite asset typing replaces the original next-env.d.ts (static image imports
// resolve to their served URL; the generated CRM stylesheet is imported with ?raw).
