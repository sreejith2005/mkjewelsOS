import {useAsyncData} from "./useAsyncData";
import {fetchEmployeeTaskProgress} from "./api";
import {Panel} from "./components";
export function EmployeeProgressPanel({context,role}:{context:Readonly<Record<string,string>>;role:string}){const allowed=["super_admin","admin","manager","hr"].includes(role);const {data,error,loading}=useAsyncData(()=>allowed?fetchEmployeeTaskProgress(context):Promise.resolve(null),[allowed,context]);if(!allowed)return null;return <Panel className="mt-5" description="Assigned, completed, and remaining work within your authorized scope." title="Employee Task Progress">{loading?<p className="py-4 text-sm text-task-text-muted">Loading employee progress…</p>:error?<p className="py-4 text-sm text-task-overdue">Employee progress could not load.</p>:data?.employees.length?<div className="overflow-x-auto"><table className="min-w-full text-sm"><thead><tr><th>Employee</th><th>Branch</th><th>Department</th><th>Assigned</th><th>Completed</th><th>Remaining</th></tr></thead><tbody>{data.employees.map(row=><tr key={row.user_profile_id}><td>{row.employee_name}</td><td>{row.branch_name}</td><td>{row.department_name??"—"}</td><td>{row.assigned}</td><td>{row.completed}</td><td>{row.remaining}</td></tr>)}</tbody></table></div>:<p className="py-4 text-sm text-task-text-muted">No employee task data in this range.</p>}</Panel>;}
